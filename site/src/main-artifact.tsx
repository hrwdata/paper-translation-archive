import React from "react";
import ReactDOM from "react-dom/client";
import { ArtifactPage } from "./app/ArtifactPage";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ArtifactPage />
  </React.StrictMode>
);
