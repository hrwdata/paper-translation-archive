import React from "react";
import ReactDOM from "react-dom/client";
import { ArtifactIndexPage } from "./app/ArtifactIndexPage";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ArtifactIndexPage />
  </React.StrictMode>
);
